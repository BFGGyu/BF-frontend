import type { NextPage } from 'next';
import React, { useEffect, useRef, useState } from 'react';
import styled from 'styled-components';

const Index: NextPage = () => {
  return (
    <>
      <ASD>지도 페이지 abcde</ASD>
    </>
  );
};

const ASD = styled.div`
  font-family: 'cursive';
  fontsize: 20;
`;
export default Index;
